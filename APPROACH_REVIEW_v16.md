# Frequency Estimation for Periodic and Rhythmic EEG Patterns: Review v16

## Critical Methodological Principle

**No method may use gold standard labels as input.** All algorithms operate from raw EEG only.

## Dataset (as of 2026-03-25)

### EEG Data

13,556 standardized .mat files (19 monopolar channels × 2000 samples at 200 Hz).

All files are monopolar (common average reference), compatible with both bipolar montage derivation and Alexandra's algorithm. Bipolar-only files have been removed.

### Label System

All labels are segment-level. The canonical file is `data/labels/segment_labels.csv` (one row per EEG file), generated by `code/data_management/build_segment_labels.py`.

| Label type | Segments | Description |
|-----------|----------|-------------|
| Vote vectors (≥10 experts) | 4,088 | Per-segment IIIC crowd votes (Jing et al. 2023) |
| Vote vectors (MW single) | ~9,500 | MW folder-assignment as single-vote vector |
| MW/expert frequency | 3,607 | Expert-reviewed frequency annotations |
| Auto-assigned frequency | 4,368 | Algorithm-assigned (HilbertCV/NVO) |
| Spatial annotations | 965 | Brain region involvement (LB, PH, SZ raters) |
| Laterality | 2,674 | Left/right/bilateral |
| Discharge timing | 2,400 | Per-discharge times (PD cases) |
| Wave timing | 549 | Per-wave times (RDA cases) |
| Channel involvement | 2,228 | Per-channel ground truth |

**Vote vector format**: Each segment has `[other, seizure, lpd, gpd, lrda, grda]` vote counts. For IIIC crowd-labeled segments, these come from 10-30 expert raters. For MW-labeled segments, these are single votes (n_votes=1). The `n_votes` column distinguishes the two.

**Data provenance**: EEG files come from three sources:
1. **IIIC crowd-labeled** (sub-S format): 10-minute recordings on S3, center 10-second segment extracted. 4,088 segments with ≥10 expert votes.
2. **MW-labeled** (sub-I and sub-S format): From pattern-specific S3 folders (LRDA/, GRDA/, etc.), single-rater classification.
3. **Expert dataset** (abn format): Original 38-patient dataset labeled by LB, PH, SZ, MW.

## What Changed Since v15

### 1. Label System Overhaul

Discovered that `patients.csv` was aggregating IIIC votes across all segments per patient, not per segment. A patient labeled "LRDA" might have had most 10-second segments voted "other" by the crowd — only specific windows showed LRDA. This was causing massive label noise.

**Fix**: Created `segment_labels.csv` as the single canonical label file (one row per EEG file on disk). Each segment has its own vote vector. `patients.csv` archived.

### 2. IIIC Segment Downloads

Downloaded 4,088 new segments with ≥10 expert votes from `s3://bdsp-opendata-credentialed/morgoth1/data/internal_dataset/IIIC/segments_raw/`. Each is a 10-minute recording; we extract the center 10-second window (the labeled segment). Files are stored as 19-channel monopolar.

| Pattern | Segments downloaded |
|---------|-------------------|
| LRDA | 363 |
| GRDA | 477 |
| LPD | 1,898 |
| GPD | 1,035 |
| Seizure | 321 |

### 3. Monopolar Conversion

All existing bipolar (18-channel) files were re-downloaded as monopolar (19-channel) from S3. 8,402 files replaced; 2,352 files without a monopolar source were deleted. All EEG is now monopolar, required for Alexandra's common-average-reference algorithm.

### 4. V4 Lateralization Contest (64 methods, clean labels)

With clean per-segment IIIC labels, hemisphere-independent methods went from AUC 0.58 → **0.84**.

**Dataset**: 5,387 segments (1,952 LRDA + 3,435 GRDA), all with per-segment vote vectors.

#### Lateralization-only results (25 methods)

| Rank | Method | AUC | HQ AUC (≥10 votes) | Cohen's d |
|------|--------|-----|---------------------|-----------|
| 1 | **L24_EnvelopeAmplitude** | **0.839** | **0.878** | 1.12 |
| 2 | L05_RMSAmplitude | 0.816 | 0.848 | 0.89 |
| 3 | L01_DeltaBandpower | 0.806 | 0.849 | 1.07 |

Simple amplitude/envelope methods dominate. LRDA has asymmetric delta power across hemispheres; GRDA is symmetric.

#### Unified methods (classification + lateralization + frequency, 40 methods)

| Rank | Method | AUC | HQ AUC | Freq ρ | Strategy |
|------|--------|-----|--------|--------|----------|
| 1 | **V12_IterativeRefine** | **0.838** | **0.884** | 0.585 | Coarse lat → freq → narrowband → refined lat |
| 2 | V25_FreqBandEnvRatio | 0.858 | 0.853 | — | Freq-band envelope ratio (no freq output) |
| 3 | V22_EnvAmp_DomHilbert | 0.814 | 0.845 | 0.636 | Envelope amp + dom-hemi Hilbert freq |
| 4 | V23_CherryPick | 0.814 | 0.845 | 0.636 | L24 score + U11 freq (independent) |
| 5 | V04_PLVSelected | 0.803 | 0.823 | **0.675** | PLV-coherent channels for score + freq |

**Key findings**:

1. **Clean labels are transformative**: V1 (noisy labels) AUC 0.58 → V4 (clean labels) AUC 0.84. The hemisphere-independent constraint works when labels are correct.

2. **Per-hemisphere Hilbert improves frequency**: Running M3_HilbertCV per-hemisphere and taking the dominant-hemisphere frequency yields ρ=0.667 (vs original 0.593). Isolating the dominant hemisphere removes contralateral noise.

3. **Lateralization-frequency tradeoff**: Best lateralizers (envelope amplitude) are different from best freq estimators (Hilbert CV). The Pareto frontier:
   - V12: AUC 0.838, Freq ρ 0.585 (best lateralization)
   - V22: AUC 0.814, Freq ρ 0.636 (balanced)
   - V04: AUC 0.803, Freq ρ 0.675 (best frequency)

4. **Iterative refinement works**: V12 first estimates frequency from the dominant hemisphere, then narrows the bandpass to that frequency, then re-computes lateralization on the narrowband signal. This removes non-RDA activity and sharpens lateralization.

### RDA Unified Architecture

```
EEG (19ch monopolar → 18ch bipolar)
│
├── Per-hemisphere processing (L: 8ch, R: 8ch)
│   │
│   ├── Envelope amplitude per channel → mean per hemisphere
│   │   → left_score, right_score
│   │   → LRDA vs GRDA (asymmetry)
│   │   → Which side (argmax)
│   │
│   └── Hilbert inst. frequency (top-3 channels per hemisphere)
│       → left_freq, right_freq
│       → Frequency estimate (from dominant hemisphere)
│
└── OUTPUT: subtype (LRDA/GRDA), side (L/R), frequency (Hz)
```

## Architecture Summary

### PD Pipeline (PDCharacterizer)
```
EEG → ChannelPD-Net (×18ch) → laterality + spatial (PLV) + timing (HemiCET+DP) + frequency
```

### RDA Pipeline (in development)
```
EEG → Per-hemisphere envelope + Hilbert → classification + lateralization + frequency
```

### BIPD Pipeline
```
EEG → L/R HemiCET+DP → 21 timing features → GBT → P(BIPD)
```

## Ten Tasks Status

| # | Task | Status | Best Method | Performance |
|---|------|--------|-------------|-------------|
| 1 | LPD vs GPD | **Done** | RF 300 | AUC 0.931 |
| 2 | LRDA vs GRDA | **Done** | Hemisphere envelope asymmetry | **AUC 0.839** (HQ: 0.878) |
| 3 | PD channel ID | Partial | CNN+Attention | AUC 0.870 |
| 4 | RDA channel ID | Pseudolabels | CNN | AUC 0.842 |
| 5 | PD discharge timing | **Done** | PDCharacterizer (HemiCET+DP) | **F1 0.684** |
| 6 | RDA wave timing | **In progress** | Auto-label+review | 549 cases labeled |
| 7 | RDA frequency | **In progress** | Per-hemi Hilbert CV | **ρ 0.667** (up from 0.593) |
| 8 | PD frequency | **Done** | PDCharacterizer (CNN+ACF→IPI) | **ρ 0.681** |
| 9 | BIPD detection | **Done** | HemiCET+GBT | AUC 0.840, Sens 63% |
| 10 | PD spatial extent | **Done** | PDCharacterizer (Hybrid-PLV) | **Composite 0.811, AUC 0.814** |

## Next Steps

1. **Frequency review viewer** — Build viewer with V22's per-hemisphere frequency estimates as defaults, narrowband overlay for quick review/correction by MW
2. **LRDA side labeling** — Label confirmed LRDA cases as left or right using symmetric channel viewer
3. **RDA localization** — Adapt PLV spatial method for RDA (which regions are involved)
4. **RDA wave timing review** — MW to review 549 auto-labeled cases
5. **Train RhythmiCET** — RDA evidence U-Net from reviewed wave labels
6. **Paper** — Write up the unified RDA characterization pipeline
